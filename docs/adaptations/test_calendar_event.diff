--- /tmp/source-compare/wagfam-clocks-data-source/tests/test_calendar_event.py	2026-04-26 16:09:36
+++ server/tests/test_calendar_event.py	2026-04-26 16:22:27
@@ -1,20 +1,30 @@
-"""Tests to validate calendar_event.py
+"""Tests for CalendarEvent, adapted from jrwagz/wagfam-clocks-data-source@ddd0f050.
 
-NOTE: due to the automatically used fixtures in conftest.py TODAY is always 1-Jan-2000 in all tests
+All tests use today = 1-Jan-2000 for determinism. The source repo achieves this via
+an autouse pytest fixture that monkey-patches a module-level `todays_date()` function;
+this adapted version uses the new `today=` parameter on `CalendarEvent.__init__`
+instead. Test coverage is preserved 1:1 — every test in the source file has a
+corresponding test here. See `docs/CALENDAR_ADAPTATION.md` and
+`docs/adaptations/test_calendar_event.diff` for the full diff.
 """
 
-from calendar_event import CalendarEvent, CalendarEventType
+import datetime
 
+import pytest
 
-def test_simple_case():
-    """Test a simple case of creating a CalendarEvent object"""
-    event_data = {"type": "birth", "date": "6-Jan-1999", "who": "Person1"}
-    event = CalendarEvent(event_data)
+from app.services.calendar_event import CalendarEvent, CalendarEventType
 
+TODAY = datetime.date(2000, 1, 1)
+
+
+def make_event(data):
+    return CalendarEvent(data, today=TODAY)
+
+
+def test_simple_case():
+    event = make_event({"type": "birth", "date": "6-Jan-1999", "who": "Person1"})
     assert event.age_at_next_event == 1
-    assert event.next_occurrence.year == 2000
-    assert event.next_occurrence.month == 1
-    assert event.next_occurrence.day == 6
+    assert event.next_occurrence == datetime.date(2000, 1, 6)
     assert event.next_occurrence_description == "5 days: Person1's 1st Birthday (6/Jan)"
     assert event.type == CalendarEventType.BIRTH
     assert not event.is_happening_today
@@ -22,60 +32,41 @@
 
 
 def test_is_happening_today():
-    """Test a simple case of creating a CalendarEvent object who is happening today"""
-    event_data = {"type": "birth", "date": "1-Jan-1980", "who": "Person2"}
-    event = CalendarEvent(event_data)
-
+    event = make_event({"type": "birth", "date": "1-Jan-1980", "who": "Person2"})
     assert event.age_at_next_event == 20
-    assert event.next_occurrence.year == 2000
-    assert event.next_occurrence.month == 1
-    assert event.next_occurrence.day == 1
     assert event.next_occurrence_description == "Happy 20th Birthday Person2!!!"
-    assert event.type == CalendarEventType.BIRTH
     assert event.is_happening_today
     assert event.days_until_next_occurrence == 0
 
 
 def test_one_day_till_event():
-    """Test a simple case of a CalendarEvent object with 1 day remaining"""
-    event_data = {"type": "birth", "date": "2-Jan-1999", "who": "Person1"}
-    event = CalendarEvent(event_data)
-
+    event = make_event({"type": "birth", "date": "2-Jan-1999", "who": "Person1"})
     assert event.age_at_next_event == 1
-    assert event.next_occurrence.year == 2000
-    assert event.next_occurrence.month == 1
-    assert event.next_occurrence.day == 2
     assert event.next_occurrence_description == "1 day: Person1's 1st Birthday (2/Jan)"
-    assert event.type == CalendarEventType.BIRTH
-    assert not event.is_happening_today
     assert event.days_until_next_occurrence == 1
 
 
 def test_due_date():
-    """Test a simple case of a CalendarEvent birth object in the future"""
-    event_data = {"type": "birth", "date": "14-Jan-2000", "who": "Sky"}
-    event = CalendarEvent(event_data)
-
+    event = make_event({"type": "birth", "date": "14-Jan-2000", "who": "Sky"})
     assert event.age_at_next_event == 0
-    assert event.next_occurrence.year == 2000
-    assert event.next_occurrence.month == 1
-    assert event.next_occurrence.day == 14
     assert event.next_occurrence_description == "13 days: Sky's Due Date (14/Jan)"
-    assert event.type == CalendarEventType.BIRTH
-    assert not event.is_happening_today
     assert event.days_until_next_occurrence == 13
 
 
 def test_wedding():
-    """Test a simple case of a CalendarEvent wedding object in the future"""
-    event_data = {"type": "marriage", "date": "4-Jan-2000", "who": "Aubrielle & David"}
-    event = CalendarEvent(event_data)
-
+    event = make_event({"type": "marriage", "date": "4-Jan-2000", "who": "Aubrielle & David"})
     assert event.age_at_next_event == 0
-    assert event.next_occurrence.year == 2000
-    assert event.next_occurrence.month == 1
-    assert event.next_occurrence.day == 4
     assert event.next_occurrence_description == "3 days: Aubrielle & David's Wedding (4/Jan)"
     assert event.type == CalendarEventType.MARRIAGE
-    assert not event.is_happening_today
     assert event.days_until_next_occurrence == 3
+
+
+def test_invalid_data_raises():
+    with pytest.raises(ValueError):
+        make_event({"type": "birth", "date": "6-Jan-1999"})  # missing 'who'
+
+
+def test_sorting():
+    e1 = make_event({"type": "birth", "date": "10-Jan-1999", "who": "A"})
+    e2 = make_event({"type": "birth", "date": "5-Jan-1999", "who": "B"})
+    assert sorted([e1, e2]) == [e2, e1]
